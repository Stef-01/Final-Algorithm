name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # Fail on high or critical advisories in what ships (moderate ones are reviewed in docs/PLAN.md).
      - run: npm audit --omit=dev --audit-level=high
      - run: npm run typecheck
      - run: npx expo lint
      - run: npm test -- --ci
      - run: npm run test:py
      - run: npm run budget
